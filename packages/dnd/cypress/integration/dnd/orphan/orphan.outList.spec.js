context("DnD/Testing single element is the list", () => {
  before(() => {
    cy.visit("http://localhost:3001/");
  });

  context("Moving element outside the list from the bottom", () => {});
  context("Moving element outside the list from the top", () => {});
  context("Moving element outside the list from the right", () => {});
  context("Moving element outside the list from the left", () => {});
  context("End transformation", () => {});
});
