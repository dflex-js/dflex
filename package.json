{
  "name": "dflex-monorepo",
  "description": "DFlex is a JavaScript Project to Manipulate DOM Elements.",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/**",
    "scripts/*"
  ],
  "repository": "https://github.com/dflex-js/dflex",
  "author": "Jalal Maskoun",
  "scripts": {
    "start": "cross-env NODE_ENV=development pnpm -F dflex-dnd-playground dev",
    "cy:dev": "cross-env NODE_ENV=development && pnpm start-server-and-test 'pnpm run start' http-get://localhost:3001 'cypress open --env extended=true --project packages/dflex-dnd-playground'",
    "test:unit": "jest",
    "test:e2e": "pnpm start-server-and-test 'pnpm run server' http-get://localhost:3001 'cypress run --env extended=true --project packages/dflex-dnd-playground'",
    "cy:run:vertical:chrome": "cypress run --env extended=true --project packages/dflex-dnd-playground --headless --spec 'packages/dflex-dnd-playground/cypress/integration/same-container-vertical/**/*.spec.ts' --config-file cypress.json --browser chrome",
    "cy:run:multi:chrome": "cypress run --project packages/dflex-dnd-playground --headless --spec 'packages/dflex-dnd-playground/cypress/integration/multiple-containers/**/*.spec.ts' --config-file cypress.json --browser chrome",
    "test:vertical:chrome": "start-server-and-test 'pnpm -F dflex-dnd-playground dev' http-get://localhost:3001 'pnpm cy:run:vertical:chrome'",
    "test:multi:chrome": "start-server-and-test 'pnpm -F dflex-dnd-playground dev' http-get://localhost:3001 'pnpm cy:run:multi:chrome'",
    "clean": "pnpm -r --parallel clean",
    "build": "cross-env NODE_ENV=production && pnpm clean && pnpm -r build",
    "check-types": "tsc --noEmit",
    "emit-types": "pnpm -r emit",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx --fix",
    "prepare-release": "changeset",
    "update-version": "pnpm changeset version && pnpm install"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "@swc/core": "^1.2.186",
    "@swc/jest": "^0.2.21",
    "@types/jest": "^27.5.1",
    "@types/node": "^17.0.34",
    "cross-env": "^7.0.3",
    "esbuild": "^0.14.39",
    "eslint-config-dflex": "workspace:*",
    "istanbul-lib-instrument": "^5.2.0",
    "jest": "^28.1.0",
    "jest-environment-jsdom": "^28.1.0",
    "prettier": "^2.6.2",
    "rimraf": "^3.0.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.4",
    "vite": "^2.9.9",
    "cypress": "^9.6.1",
    "start-server-and-test": "^1.14.0"
  }
}
