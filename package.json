{
  "name": "dflex-monorepo",
  "description": "DFlex is a JavaScript Project to Manipulate DOM Elements.",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/**",
    "scripts/*"
  ],
  "repository": "https://github.com/dflex-js/dflex",
  "author": "Jalal Maskoun",
  "scripts": {
    "start": "cross-env NODE_ENV=development pnpm -F dflex-dnd-playground dev",
    "cy:dev": "cross-env NODE_ENV=development && pnpm start-server-and-test 'pnpm run start' http-get://localhost:3001 'cypress open --env extended=true --project packages/dflex-dnd'",
    "test:unit": "jest",
    "test:e2e": "pnpm start-server-and-test 'pnpm run server' http-get://localhost:3001 'cypress run --env extended=true --project packages/dflex-dnd'",
    "build": "cross-env NODE_ENV=production && pnpm -r build",
    "bump": "pnpm changeset version && pnpm install"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "@swc/core": "^1.2.186",
    "@swc/jest": "^0.2.21",
    "@types/jest": "^27.5.1",
    "@types/node": "^17.0.34",
    "cross-env": "^7.0.3",
    "esbuild": "^0.14.39",
    "eslint-config-dflex": "workspace:*",
    "istanbul-lib-instrument": "^5.2.0",
    "jest": "^28.1.0",
    "jest-environment-jsdom": "^28.1.0",
    "prettier": "^2.6.2",
    "rimraf": "^3.0.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.4",
    "vite": "^2.9.9",
    "cypress": "^9.6.1",
    "start-server-and-test": "^1.14.0"
  }
}
